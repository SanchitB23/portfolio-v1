import React from 'react';


const Test: React.FC = (props) => {
    console.log(props)

    return (
        // <Loader finishLoading={(v) => console.log("finish loading", v)}/>
        <div>Mocks</div>
    );
};
export default Test;
